spring.profiles.active: local

server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://some-mysql:3306/management?useSSL=false
    username: root
    password: Seguridad_6
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: none

logging:
  level:
    root: INFO